import React from 'react';
import './header.scss';

export const Header = () => {
  return (
    <header className="app-header">
      <code className="header-text">big idea</code>
    </header>
  );
};
